#include <stdio.h>

int lnko(int a, int b){
	int i;
	
	a = ( a > 0) ? a : -a;
	b = ( b > 0) ? b : -b;
	
	while(a!=b){
        if(a > b)
            a -= b;
        else
            b -= a;
    }
    return a;
}

int main(){
	int n1, n2;
	printf("Adja meg az elso szamot: ");
	scanf("%d", &n1);
	printf("\nAdja meg a masodik szamot: ");
	scanf("%d", &n2);
	printf("\nLegnagyobb kozos oszto: %d", lnko(n1, n2));
	return 0;
}



Működik negatív inputtal is!
igy már tokéletes koszii
